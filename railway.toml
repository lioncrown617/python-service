[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn odd:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 0 --keep-alive 65 --worker-class gthread"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
